rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // POS Restaurant System - Accessible by restaurant POS instances (anonymous auth or public POS)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
